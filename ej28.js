// Crear un documento con el nombre ej28.js
// Declarar la variable numero y asignar el valor 9
// Mostrar en consola la tabla del 9 de 1 a 10 usando la variable numero
let numero = 9;
console.log(`En base a la tabla del ${numero}: 1 * ${numero} = ${1 * numero}`);
console.log(`En base a la tabla del ${numero}: 2 * ${numero} = ${2 * numero}`);
console.log(`En base a la tabla del ${numero}: 3 * ${numero} = ${3 * numero}`);
console.log(`En base a la tabla del ${numero}: 4 * ${numero} = ${4 * numero}`);
console.log(`En base a la tabla del ${numero}: 5 * ${numero} = ${5 * numero}`);
console.log(`En base a la tabla del ${numero}: 6 * ${numero} = ${6 * numero}`);
console.log(`En base a la tabla del ${numero}: 7 * ${numero} = ${7 * numero}`);
console.log(`En base a la tabla del ${numero}: 8 * ${numero} = ${8 * numero}`);
console.log(`En base a la tabla del ${numero}: 9 * ${numero} = ${9 * numero}`);
console.log(`En base a la tabla del ${numero}: 10 * ${numero} = ${10 * numero}`);